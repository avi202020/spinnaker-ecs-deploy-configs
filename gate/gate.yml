redis:
  connection: redis://${SPINNAKER_REDIS_HOST}:6379
  configuration:
    secure:
      true

services:
  orca:
    baseUrl: http://${SPINNAKER_ORCA_HOST}:8083
  front50:
    baseUrl: http://${SPINNAKER_FRONT50_HOST}:8080
  rosco:
    enabled: false
  deck:
    baseUrl: http://${SPINNAKER_DECK_HOST}
  clouddriver:
    baseUrl: http://${SPINNAKER_CLOUDDRIVER_HOST}:7002
  igor:
    enabled: true
    baseUrl: http://${SPINNAKER_IGOR_HOST}:8088
  echo:
    enabled: true
    baseUrl: http://${SPINNAKER_ECHO_HOST}:8089

